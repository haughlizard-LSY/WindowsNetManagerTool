# 本地网卡管理工具 —— 需求理解文档（v1.0 已确认）

> 本文档为需求确认稿。用户已逐项确认关键决策（见文末"确认记录"）。

---

## 一、一句话目标

用 **Python** 实现一个 **Windows 桌面 GUI 工具**（网卡管理器），让用户能：查看本机所有网卡及其当前网络配置、为每块网卡维护多个"配置档案"（Profile），并在界面上一键把选中的配置档案**应用/切换**到对应网卡上，使其立即生效。

## 二、功能需求分解

### 功能 1：列出本机所有网卡及当前配置
- 枚举本机所有网卡（有线以太网、WLAN、虚拟网卡等），按网卡分组展示。
- 展示每块网卡的**当前生效配置**：
  - 网卡名称 / 描述、接口类型（有线/WLAN/虚拟）
  - 连接状态（已连接 / 已断开 / 已禁用）
  - MAC 地址
  - IP 获取方式（静态 Static / DHCP）
  - 当前 IP 地址、子网掩码、默认网关、DNS 服务器
  - 网卡启用/禁用状态
- 数据实时刷新（提供"刷新"操作，切换配置后自动刷新）。

### 功能 2：编辑网卡配置 / 管理多个配置档案
- 每块网卡可以保存 **多个命名配置档案**（如："办公室-静态IP"、"家里-DHCP"、"会议室-静态"）。
- 一个配置档案是绑定到某块网卡的一套完整网络参数：
  - 档案名称
  - IP 获取方式：**DHCP 自动获取 / 手动静态**（两者都支持，已确认）
  - 静态模式下：IP 地址、子网掩码（或前缀长度）、默认网关、首选/备用 DNS
  - 备注（可选）
- 支持档案的 **新增 / 修改 / 删除 / 重命名**。
- 档案数据**本地持久化**（SQLite 或 JSON 文件），重启工具后仍在，与 Windows 系统配置分离保存。

### 功能 3：选中并切换网卡配置
- 界面操作流：选择网卡 → 查看该网卡的档案列表 → 勾选/选中一个档案 → 点击"应用此配置"。
- 工具将档案参数**实际写入 Windows 网络配置**并使网卡立即生效（静态 ↔ DHCP 可互相切换）。
- 应用结果反馈：成功/失败及原因（参数非法、无权限、网卡被占用、失败回滚）。
- 切换会中断当前网络连接 → 应用前弹出**二次确认提示**。

## 三、技术方案（已确认）

| 项 | 决策 |
|---|---|
| 平台 | **仅 Windows** |
| 界面 | **桌面 GUI，使用 PySide6 (Qt for Python)** |
| 后端/系统接口 | Python 调用系统能力：`netsh` 命令族、PowerShell `Get-NetIPConfiguration`/`New-NetIPAddress`/`Set-NetIPAddress`/`Set-DnsClientServerAddress` 等，辅以 WMI/CIM 枚举 |
| 管理员权限 | **自动请求提权**：检测到非管理员运行时，通过 UAC 弹窗重新以管理员身份启动自身 |
| 配置存储 | 本地文件（SQLite 优先，便于多表/备份；或 JSON 简化），存于程序数据目录 |
| 打包 | （待定）PyInstaller 打包为 exe，可选 |

## 四、关键设计点

1. **权限模型**：读操作无需提权；写操作（应用配置、改启停）需要管理员。策略：启动即检测权限 → 非管理员时自动 UAC 提权重启；若用户拒绝，工具以只读模式运行（可查看、编辑档案，但不能应用）。
2. **切换动作实现**（Windows，管理员态）：
   - 切到静态：`Set-NetIPInterface -Dhcp Disabled` → `New-NetIPAddress`（已存在则 `Set-NetIPAddress`）→ `Set-DnsClientServerAddress`。
   - 切到 DHCP：`Set-NetIPInterface -Dhcp Enabled` → `Remove-NetIPAddress`/`Set-DnsClientServerAddress -ResetServerAddresses`。
   - 执行后校验生效结果，失败给出原因。
3. **网卡标识**：以 InterfaceIndex / InterfaceAlias 作为网卡与档案绑定的稳定键。
4. **安全**：应用前二次确认；档案修改不直接改系统，仅"应用"时写入系统。
5. **GUI 布局草案**：
   - 主窗口：左侧网卡列表（含当前状态摘要）→ 右侧上下分区：上方"当前配置"只读详情；下方"配置档案"列表（含当前应用中的档案标记）。
   - 工具栏：刷新、新增档案、编辑、删除、应用选中配置、启用/禁用网卡。
   - 编辑对话框：档案名 + 获取方式单选（DHCP/静态）+ 静态参数表单 + 备注。
   - 状态栏/日志区：显示最近操作结果。

## 五、范围外（默认不做）

- 不做远程设备管理、流量监控、WiFi 密码、驱动管理。
- 不支持网卡拓扑编辑、VLAN 配置等高级项。

---

## 确认记录（v1.0）

- [x] 界面形式：**桌面 GUI**（非 Web/命令行）
- [x] GUI 框架：**PySide6**
- [x] 目标平台：**仅 Windows**
- [x] 权限处理：**自动请求提权（UAC）**，拒绝时降级为只读模式
- [x] 配置范围：**静态 IP 与 DHCP 档案均支持**
- [x] 每网卡多档案、档案本地持久化、切换即时生效、应用前二次确认
